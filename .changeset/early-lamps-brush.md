---
"@mammothb/pi-eval": minor
---

 Add user-configurable `pythonPath` and `nodeModulesPath` via config files (`~/.pi/agent/pi-eval.json` and `.pi/pi-eval.json`) instead of requiring them as tool parameters on every invocation.

