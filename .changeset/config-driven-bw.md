---
"@mammothb/pi-permissions": minor
---

Add `bw` CLI — config-driven bwrap sandbox for pi. Replaces the old bash script with a Node.js CLI supporting JSON config files, `binds` / `binds_extra` merge strategy, WSL2 auto-detection, env var injection with `$VAR` host references, and custom PATH entries.
