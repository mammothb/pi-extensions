---
"@mammothb/pi-eval": patch
---

Move internal modules into `src/lib/` for consistency with other packages in the monorepo (`pi-ghsearch`, `pi-webfetch`, `pi-websearch` already follow this pattern)
